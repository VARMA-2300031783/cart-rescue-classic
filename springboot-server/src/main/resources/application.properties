# Spring Boot Configuration for Cart Rescue
server.port=8090

# ========================================================
# MySQL Database Configuration (Configured with your MySQL Password)
# ========================================================
spring.datasource.url=jdbc:mysql://localhost:3306/cart_rescue_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=2300031783
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings (Automatically creates database tables & schema in MySQL)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# CORS Configuration
spring.mvc.cors.allowed-origins=*
